# Temporary Deployment

Current temporary dashboard:

```text
https://c9491bd1fdf9ae.lhr.life/ui
```

Previous localtunnel dashboard:

```text
https://honest-sheep-matter.loca.lt/ui
```

API base:

```text
https://c9491bd1fdf9ae.lhr.life/v1
```

Health:

```text
https://c9491bd1fdf9ae.lhr.life/health
```

Config status:

```text
https://c9491bd1fdf9ae.lhr.life/config/status
```

Safety state:
- Provider mode: `mock`.
- Cloud forwarding: disabled.
- Prompt storage: disabled.
- No paid AI API calls are made by default.

This is a temporary localtunnel deployment. It stays live only while the local server and tunnel processes remain running on this machine.
